#include "Player.h"

namespace Domain {
    namespace Enumeration {
        const Player Player::WHITE = Player("J1");
        const Player Player::BLACK = Player("J2");
        const Player Player::NONE = Player("DRAW");
    }
}
